const initialState = {
  user: "Tamar Jacobs",
  email: "tajacobs@deloitte.com.au"
}

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
}